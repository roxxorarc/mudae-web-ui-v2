services:
  frontend-web:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    # No host port: Dokploy/Traefik routes to the container port directly
    # (5175). Add a domain in Dokploy targeting service frontend-web:5175.
    expose:
      - "5175"
    restart: unless-stopped
